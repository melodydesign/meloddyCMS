# Задачи: Редизайн Дашборда (Premium Glassmorphism)

- `[/]` 1. **Подготовка и фоны**
  - Добавить `ambient-orbs` в `dashboard.html`.
  - Прописать стили для орбов в `style.css`.
- `[ ]` 2. **Карточки статистики**
  - Обновить HTML структуру карточек в `dashboard.html`, добавив SVG-иконки.
  - Написать CSS классы `.stat-card-glass` с backdrop-filter и градиентными рамками.
- `[ ]` 3. **Вкладки клиентов (Активные проекты)**
  - Вернуть кнопкам клиентов растягивание (flex: 1) в `dashboard.js`.
  - Добавить ограничение `min-width` и горизонтальный скролл на случай большого количества клиентов.
  - Обновить стили "чипсов" на более премиальные.
- `[ ]` 4. **Пустые состояния (Empty states)**
  - Заменить унылые тексты "Нет проектов" на красивые UI-элементы в `dashboard.js`.
- `[ ]` 5. **Деплой и сброс кэша**
  - Залить файлы на сервер.
  - Вызвать `cache_bust.py`.
