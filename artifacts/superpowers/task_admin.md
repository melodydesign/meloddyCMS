# Задачи: Доработка визуала и функционала (Часть 2)

- `[/]` 1. **Исправление верстки вкладок (Дашборд)**
  - Убрать `overflow` или исправить padding у вкладок проектов.
  - Настроить `flex-wrap` и убрать принудительное растягивание (flex: 1), чтобы они шли друг за другом.
  - Растянуть пустой стейт на всю ширину (`grid-column: 1 / -1` или 100% width).
- `[ ]` 2. **Устранение "моргания" меню**
  - Добавить `style="display: none;"` к Базе знаний и Тарифу в `dashboard.html`, `profile.html`, `admin.html`, `settings.html`.
  - В JS показывать их только для клиентов `if (!isAdmin && !isDeveloper)`.
- `[ ]` 3. **Премиум-визуал для Профиля и Админки**
  - Обернуть блоки профиля в `.stat-card-glass`.
  - Обернуть таблицы админки в `.stat-card-glass`. Убрать текст "Загрузка...".
- `[ ]` 4. **Отключение сайтов**
  - Добавить роут `POST /api/admin/sites/:id/toggle` в `server.js` (или `src/routes/admin.js`).
  - Реализовать кнопку "Выключить" во вкладке "Все сайты" (`admin.js`).
- `[ ]` 5. **Деплой**
  - Залить файлы.
  - Выполнить сброс кэша (`cache_bust.py`).
